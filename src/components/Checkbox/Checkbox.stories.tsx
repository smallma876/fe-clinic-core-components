import { Checkbox } from "./Checkbox";

const meta = {
    title: "Checkbox",
    component: Checkbox
}

export default meta;

export const Base = {
    args: {
        id: "First",
        label: "first",
    }
}