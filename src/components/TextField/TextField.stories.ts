import TextField from "./TextField";

const meta = {
    title: "TextField",
    component: TextField,
}

export default meta;

export const Base = {
    args: {
        id: "TextField",
        label: "Ingresa un texto",
    } 
}